# .knowledge

Knowledge Engine 使用的机器数据目录：同步状态、索引、ID 映射等。

- 由工具生成和维护，通常不需要人工编辑。
- 例如阶段 05（Notion 同步）会在此产出 `notion-map.json`（`kb_id → notion_page_id → content_hash`）。

见 [`Plan.md`](../Plan.md) 中相关说明。
